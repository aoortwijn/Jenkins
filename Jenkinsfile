pipeline {
    agent any
    stages {
        stage('Test') {
            steps {
                echo 'Placeholder'
            }
        }
    }
    post {
        always {
            echo 'This will always run'
        }
    }
}
